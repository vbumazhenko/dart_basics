import 'dart:math';

import 'delimiters_calculator.dart';
import 'ext_num.dart';
import 'find_numbers.dart';
import 'number_system.dart';
import 'numbers_collection.dart';
import 'point.dart';
import 'user.dart';
import 'words_collection.dart';

void main() {
  
  // 1
  // print(DelimitersCalculator.gcd(10, 30));
  // print(DelimitersCalculator.lcm(10, 30));
  // print(DelimitersCalculator.factorize(9988));

  // 2
  // print(NumberSystem.toBinary(443));
  // print(NumberSystem.toDecimal("110111011"));

  // 3
  // var str = 'Однажды в студеную зимнюю 9 я из лесу вышел был сильный 3.14';
  // print(FindNumbers(str).find());

  // 4
  // var words = ['hello', 'world', 'world', 'dart'];
  // print(WordsCollection(words).getMap());

  // 5
  // var words = ['hello', 'world', 'one', 'two', 'three', 'zero', 'zero', 'dart'];
  // print(NumbersCollection(words).getNumbers());

  // 6
  // var point1 = Point.root();
  // print(point1.distanceTo(Point(5, 5, 5)));

  // var p1 = Point(0, 0, 0);
  // var p2 = Point(10, 0, 0);
  // var p3 = Point(0, 10, 0);
  // print(Point.triangleArea(p1, p2, p3));

  // 7
  // num x = 25;
  // print(x.rootN(2));

  // 8
  // var user1 = AdminUser('info@mail.ru');
  // print(user1.getMailSystem());

  // var um = UserManager<User>();
  // um.add(User('1@mail.ru'));
  // um.add(AdminUser('2@mail.ru'));
  // um.add(GeneralUser('3@mail.ru'));
  // um.printEmailList();

}
